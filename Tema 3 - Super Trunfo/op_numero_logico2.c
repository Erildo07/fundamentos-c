#include<stdio.h>
int main()
{
    int a = 1;

    if (!a) {//(!(a > 0)
        printf("A variavel e um numero positivo:");

    } else {
        printf("A variavel e diferente de zero:");
    }


    return 0;
}
